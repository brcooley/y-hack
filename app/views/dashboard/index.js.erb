<script>
	$(document).ready(function() {
		// Save task
		var savebtn = $('#save-btn')
		savebtn[0].onclick = null;
		savebtn.click(function() {
			var event = {
				'name': $('#newtask > .name').html(),
				'tag': $('#newtask > .tag').html(),
				'guess': $('#newtask > .guess').html()
			}
			$.ajax({
				type: 'POST',
				url: '/users/' + cur_user + '/tasks',
				data: event,
				success: function() {
					console.log('Task saved');
				}
			});
		});
	})
</script>
